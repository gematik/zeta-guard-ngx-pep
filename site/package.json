{
  "name": "ngx_pep",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.mjs --watch --serve",
    "ensure_env": "[[ -f ./.env ]] || install -v -m 00600 .env.tpl .env",
    "build": "npm run ensure_env; set -a; . $PWD/.env; set +a; prettier --write . && printf \"\\n\" && node esbuild.mjs",
    "clean": "rm -rf public/js",
    "prefix_install": "mkdir -p ../prefix/html/ && rm -rf ../prefix/html/* && cp -a public/* ../prefix/html/ && printf \"../site/public/* → ../prefix/html/\\n\""
  },
  "dependencies": {
    "keycloak-js": "^26.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "prettier": "3.6.2",
    "typescript": "^5.9.2"
  }
}
